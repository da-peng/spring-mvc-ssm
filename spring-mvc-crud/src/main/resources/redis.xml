<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd">
    <context:component-scan base-package="com.qlchat.cache" />
    <!-- Redis 配置 -->
    <bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <!--控制一个pool可分配多少个jedis实例-->
        <property name="maxTotal" value="${QLCHAT_REDIS_POOL_MAXACTIVE}" />
        <!--控制一个pool最多有多少个状态为idle(空闲)的jedis实例；-->
        <property name="maxIdle" value="${QLCHAT_REDIS_POOL_MAXIDLE}" />
        <property name="minIdle" value="1" />
        <!--表示当borrow一个jedis实例时，最大的等待时间，如果超过等待时间，则直接抛出JedisConnectionException-->
        <property name="maxWaitMillis" value="${QLCHAT_REDIS_POOL_MAXWAIT}" />
        <!--在borrow一个jedis实例时，是否提前进行alidate操作；如果为true，则得到的jedis实例均是可用的；-->
        <property name="testOnBorrow" value="${QLCHAT_REDIS_POOL_TESTONBORROW}"/>
    </bean>

    <bean id="tempJedisPool" class="redis.clients.jedis.JedisPool" destroy-method="destroy">
        <constructor-arg ref="jedisPoolConfig" />
        <constructor-arg type="java.lang.String" value="${QLCHAT_REDIS_MASTER_IP}" />
        <constructor-arg type="int" value="${QLCHAT_REDIS_MASTER_PORT}" />
        <constructor-arg type="int" value="30000" />
        <constructor-arg value="#{'${QLCHAT_REDIS_MASTER_PASSWORD}'!=''?'${QLCHAT_REDIS_MASTER_PASSWORD}':null}" />
    </bean>
</beans>